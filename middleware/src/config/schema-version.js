'use strict';

/**
 * Schema version of this release: the highest schema_version registered by
 * backend/db/postgres/schema/*.sql (checked by schema-version.test.js).
 */
module.exports = { SCHEMA_VERSION: '3.9.0' };
