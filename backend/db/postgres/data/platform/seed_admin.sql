-- =============================================================================
-- SERVICE CATALOGUE — PostgreSQL platform seed
-- data/platform/seed_admin.sql
-- Intentionally no-op.
--
-- Clean bootstrap must create the first administrator account in the install
-- wizard or via an explicit operator-provisioned admin flow.
-- =============================================================================
