-- =============================================================================
-- PostgreSQL test seeds
--
-- The canonical init path uses /app/middleware/src/scripts/seed-demo-data.js
-- so the install wizard and clean init deployment share the same demo logic.
-- This file remains only as a documentation stub for the DB layer.
-- =============================================================================

SELECT 'Test seeds are applied through middleware/src/scripts/seed-demo-data.js.' AS info_message;
